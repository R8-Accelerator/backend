'use strict';

/**
 * accelerator-invite-code controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::accelerator-invite-code.accelerator-invite-code');

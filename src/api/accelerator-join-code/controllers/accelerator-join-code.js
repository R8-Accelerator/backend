'use strict';

/**
 * accelerator-join-code controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::accelerator-join-code.accelerator-join-code');

'use strict';

/**
 * cohort-application router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::cohort-application.cohort-application');

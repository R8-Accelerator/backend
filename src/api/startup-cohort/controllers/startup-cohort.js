'use strict';

/**
 * startup-cohort controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::startup-cohort.startup-cohort');
